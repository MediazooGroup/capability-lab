---
name: build-web-presentation
description: Create a timed interactive web presentation and speaker script from the session brain. Use for rebuilding or adapting this workshop.
---

# Build web presentation

Read `../../second-brain/01-session/brief.md`, `session.json` in that folder, the method notes and `../../second-brain/04-brand/design.md`. Preserve the requested duration and audience themes. Use the learning example as the centrepiece. The last reveal must show the real brain, skills and assets that the audience receives, and distinguish what was prebuilt from any live model work.

Author or revise session.json as the single ordered stage source. Each frame needs a title, duration in seconds, a visual type, speaker notes and an audience/presenter cue. Sum the durations to 1,800 seconds for this brief. Keep one idea visible per frame and use the connected map between topics. Build a full-screen click-through web presentation from this content. Use animated scene transitions, interactive diagrams, presenter notes and keyboard controls. Deliver the presentation inside the app rather than a PowerPoint deck. A self-contained HTML copy is the offline fallback.

Inspect every rendered frame at the intended projector ratio. Rehearse the click path and all keyboard controls. Include the speaker introduction with an actual supplied or verified portrait. Let the presenter open the live board from a clickable picture. Show the real brain, skill and output, then a specific refinement. Provide three distinct QRs for the board, brain and GitHub, with simple download and launch steps. Confirm each destination opens without signing in. Use `../check-session/SKILL.md` for final checks. Update the build receipt with actual outputs and tests; never invent evidence.
