import {makeSkill,type Workbook} from './model';
type Tool={name:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean;untrustedContentHint:boolean};execute:(input:unknown)=>unknown};
type Context={registerTool:(tool:Tool,options:{signal:AbortSignal})=>void|Promise<void>};
export function registerRecipeTool(context:Context|undefined,getWork:()=>Workbook){if(!context?.registerTool)return()=>{};const lifecycle=new AbortController();const tool:Tool={name:'read_skill_recipe',description:'Return the current visible personal skill as editable Markdown. Does not run AI, download a file or change your work.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute(input){if(input===null||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw new Error('Expected an empty object.');return{filename:'SKILL.md',markdown:makeSkill(getWork())}}};try{void Promise.resolve(context.registerTool(tool,{signal:lifecycle.signal})).catch(()=>{})}catch{/* Optional browser capability. The visible UI remains usable. */}return()=>lifecycle.abort()}
